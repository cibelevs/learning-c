//2. Soma de números pares
//Escreva uma função que retorna a soma dos números pares de 1 a 100 utilizando um laço for.
#include<stdio.h>
int main (void)
{
 int soma = 0;
    for (int i= 1; i<=100; i++)
    {
        if (i % 2 == 0)
        {
            soma = soma + i;
        }
    }
    printf("Soma total: %d", soma);
    return 0;
}
